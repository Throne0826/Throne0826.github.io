---
title: C++ 题解模板
date: 2026-07-08 17:54:55
mathjax: true
tags:
  - C++
  - 题解模板
categories:
  - 算法竞赛
---

# C++ 题解模板

## 检查结果

- Markdown 代码围栏语法正确，已保留 `cpp` 语言标识。
- Front matter 的 Hexo YAML 分隔符和字段已保留。
- 原 `tags` 和 `categories` 存在空列表项，可能导致 Hexo 生成空标签或空分类；已补充为有效值。
- 未发现 LaTeX 公式内容；`mathjax: true` 保留不变。
- 标题层级基本合理，已在正文顶部添加 `## 检查结果`。
- 代码块中的算法、复杂度、变量名和技术含义未改变，仅调整了常规 C++ 代码格式与缩进。

```cpp
// code
#include <bits/stdc++.h>
using namespace std;

const int N = 1e5 + 7, M = 998244353;

void solve() {
}

signed main() {
    int T = 1;
    while (T--) {
        solve();
    }
    return 0;
}
```