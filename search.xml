<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>C++ 题解模板</title>
    <url>/2026/07/08/2026-07-08-%E6%96%B0%E6%96%87%E7%AB%A0/</url>
    <content><![CDATA[<h1 id="C-题解模板"><a href="#C-题解模板" class="headerlink" title="C++ 题解模板"></a>C++ 题解模板</h1><h2 id="检查结果"><a href="#检查结果" class="headerlink" title="检查结果"></a>检查结果</h2><pre><code class="lang-cpp">// code
#include &lt;bits/stdc++.h&gt;
using namespace std;

const int N = 1e5 + 7, M = 998244353;

void solve() &#123;
&#125;

signed main() &#123;
    int T = 1;
    while (T--) &#123;
        solve();
    &#125;
    return 0;
&#125;
</code></pre>
]]></content>
      <categories>
        <category>算法竞赛</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>题解模板</tag>
      </tags>
  </entry>
  <entry>
    <title>2024CCPC深圳站组队赛题解</title>
    <url>/2024/10/17/2024CCPC%E6%B7%B1%E5%9C%B3%E7%AB%99%E7%BB%84%E9%98%9F%E8%B5%9B%E9%A2%98%E8%A7%A3/</url>
    <content><![CDATA[<h2 id="A：一道好题"><a href="#A：一道好题" class="headerlink" title="A：一道好题"></a>A：一道好题</h2><h3 id="题目描述"><a href="#题目描述" class="headerlink" title="题目描述"></a>题目描述</h3><p>有一个长度为 $n$、初始全为 $0$ 的序列 $a$，另有一个长度为 $n$ 的序列 $b$。目标是通过若干次操作将 $a$ 变成 $b$。</p>
<span id="more"></span>
<p>可以执行两种操作：</p>
<ol>
<li><code>1 x</code>：将 $a$ 中所有值为 $x$ 的数加 $1$。</li>
<li><code>2 x</code>：将 $a$ 中下标为 $x$ 的数加 $1$。</li>
</ol>
<p>不需要最小化操作次数，但操作次数不能超过 $20000$。</p>
<h3 id="输入"><a href="#输入" class="headerlink" title="输入"></a>输入</h3><p>第一行一个正整数 $n$，满足 $1 \le n \le 1000$。</p>
<p>第二行 $n$ 个非负整数 $b_1,b_2,\cdots,b_n$，满足 $0 \le b_i \le n$。</p>
<h3 id="输出"><a href="#输出" class="headerlink" title="输出"></a>输出</h3><p>第一行输出操作次数 $k$，需要满足 $0 \le k \le 20000$。</p>
<p>之后 $k$ 行，每行输出一种操作。对于 <code>1 x</code>，需要满足 $0 \le x \le n$；对于 <code>2 x</code>，需要满足 $1 \le x \le n$。</p>
<h3 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h3><p>一开始尝试过贪心和分块，但容易超过操作次数限制。注意到 $20000$ 大约可以支持 $O(n\log n)$ 级别的构造，因此可以考虑分治。</p>
<p>在区间 $[1,n]$ 中，先把后半部分 $[(n+1)/2,n]$ 依次加一，这样整体会被分成两个不同部分，再配合第一种操作对整段统一加一。递归处理左右两段，就能把操作次数控制在可接受范围内。</p>
<p>核心思路是：用“整体加一”降低单点操作次数，再通过分治把不同值逐层拆开。</p>
<h2 id="I：不定方程"><a href="#I：不定方程" class="headerlink" title="I：不定方程"></a>I：不定方程</h2><h3 id="题目描述-1"><a href="#题目描述-1" class="headerlink" title="题目描述"></a>题目描述</h3><p>给定正整数 $n,k$，求不定方程</p>
<script type="math/tex; mode=display">
a^k-b^k=n</script><p>的正整数解数量。</p>
<h3 id="输入-1"><a href="#输入-1" class="headerlink" title="输入"></a>输入</h3><p>第一行一个整数 $T$，表示询问数量，满足 $1 \le T \le 20$。</p>
<p>之后 $T$ 行，每行两个整数 $n,k$，表示一次询问，满足 $1 \le n \le 10^{18}$，$3 \le k \le 64$。</p>
<h3 id="输出-1"><a href="#输出-1" class="headerlink" title="输出"></a>输出</h3><p>对于每一组询问，输出一行一个非负整数，表示答案。</p>
<h3 id="分析-1"><a href="#分析-1" class="headerlink" title="分析"></a>分析</h3><p>利用恒等式：</p>
<script type="math/tex; mode=display">
a^k-b^k=(a-b)(a^{k-1}+a^{k-2}b+\cdots+b^{k-1})</script><p>设 $c=a-b$，也就是 $a=b+c$，带入后得到：</p>
<script type="math/tex; mode=display">
(b+c)^k-b^k=n</script><p>可以看出，$a-b$ 是 $n$ 的一个因子；同时在 $c$ 固定时，$(b+c)^k-b^k$ 关于 $b$ 单调递增。</p>
<p>因此可以枚举 $n$ 的因子，再二分寻找对应的 $b$。由于 $n$ 很大，可以使用 <code>Pollard-Rho</code> 分解质因子，再枚举因子组合。</p>
<h3 id="代码"><a href="#代码" class="headerlink" title="代码"></a>代码</h3><pre><code class="lang-cpp">int n, a[N], c[N];
PII b[N];
vector&gt; op;
void merge(int l, int r, int val)//val：此时整个区间的值
&#123;
    if (b[r].f == val)
        return;
    int mid = l + r + 1 &gt;&gt; 1, now = val;
    if (b[mid].f &gt; val)
    &#123;
        for (int i = mid; i  now)
        merge(mid, r, now);
    merge(l, mid - 1, val);
&#125;
void solve()
&#123;
    cin &gt;&gt; n;
    for (int i = 1; i &gt; a[i], b[i] = &#123;a[i], i&#125;;

    sort(b + 1, b + 1 + n);
    merge(1, n, 0);

    cout v[N];
int p[N], ff = 0, q[N], cnt = 0;
bool st[N];

void dfs(int u, int f)
&#123;
    for(int it : v[u])
    &#123;
        if(it == f) continue;
        if(ff) return;
        p[it] = u;
        // cout &gt; n;
    for(int i = 1; i &gt; x &gt;&gt; y;
        v[x].push_back(y);
        v[y].push_back(x);
        num[x] ++;
        num[y] ++;
    &#125;
    dfs(1, 0);
    q[cnt + 1] = q[1];
    int cnt1 = 0, cnt2 = 0;
    for(int i = 1; i &gt; n &gt;&gt; q &gt;&gt; m &gt;&gt; k;
    p[0] = 1;
    // p2[0] = 1;
    for(int i = 1; i &gt; s[i];
        int z = s[i].size();
        s[i] = &#39; &#39; + s[i];
        for(int j = 1; j &gt; c[i];
        int z = c[i].size();
        c[i] = &#39; &#39; + c[i];
        for(int j = 1; j &gt; 1;
                    if(check(l, mid, i, j)) l = mid + 1;
                    else r = mid;
                &#125;
                if(c[i][l] != s[j][l])
                &#123;
                    cnt ++;
                &#125;
                lr = l + 1;
                if(cnt &gt; k) break;
            &#125;
            if(cnt &gt;=1;
    &#125;
    return res;
&#125;
void solve()&#123;
    int m,k;cin&gt;&gt;m&gt;&gt;k;
    for(int i=1,sum=1;i&lt;=k;i++,sum+=m-1,sum%=P)&#123;
        int ver=f1[i-1]*qmi(sum,P-2)%P;
        f2[i]=f2[i-1]+ver;
        f2[i]%=P;
        f1[i]=(f1[i-1]+(ver+1)*m%P-ver+P)%P;
    &#125;
    cout&lt;&lt;(f1[k]+f2[k])%P&lt;&lt;&quot;\n&quot;;
&#125;
</code></pre>
]]></content>
  </entry>
  <entry>
    <title>补题</title>
    <url>/2024/09/15/%E8%A1%A5%E9%A2%98/</url>
    <content><![CDATA[<p><a href="https://www.luogu.com.cn/problem/P2508">P2508 [HAOI2008] 圆上的整点 - 洛谷</a></p>
<span id="more"></span>
<h3 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h3><p>圆上的整点满足：</p>
<script type="math/tex; mode=display">
y^2=r^2-x^2=(r-x)(r+x)</script><p>设</p>
<script type="math/tex; mode=display">
d=\gcd(r-x,r+x), \quad r-x=ud, \quad r+x=vd</script><p>则原式变为：</p>
<script type="math/tex; mode=display">
y^2=d^2uv</script><p>同时可以得到：</p>
<script type="math/tex; mode=display">
x=\frac{v-u}{2}d, \quad 2r=(u+v)d</script><p>如果直接枚举 $2r$ 的约数 $d$，再枚举 $u$，复杂度仍然不能接受。</p>
<p>注意到 $u$、$v$ 互质，并且等式左边为平方数，所以 $u$、$v$ 都应为平方数。不妨设：</p>
<script type="math/tex; mode=display">
u=s^2, \quad v=t^2</script><p>则有：</p>
<script type="math/tex; mode=display">
x=\frac{t^2-s^2}{2}d, \quad 2r=(s^2+t^2)d</script><p>此时枚举 $2r$ 的约数 $d$，再枚举 $s$ 即可。约数数量较小，整体复杂度约为：</p>
<script type="math/tex; mode=display">
O(\sqrt r \cdot 2^{10})</script><p>实际复杂度会比这个上界更小，可以通过。</p>
<h3 id="代码"><a href="#代码" class="headerlink" title="代码"></a>代码</h3><pre><code class="lang-cpp">int ans,R;
void get(int d,int r)&#123;
    // cout0&amp;&amp;y&gt;0&amp;&amp;x*x+y*y==(R/2)*(R/2))ans+=2;
    &#125;
&#125;
void solve()&#123;
    int r;cin&gt;&gt;r;
    r&lt;&lt;=1;
    R=r;
    for(int i=1;i&lt;=r/i;i++)&#123;
        if(r%i==0)get(i,r/i);
        if(r%i==0&amp;&amp;i*i!=r)get(r/i,r/(r/i));
    &#125;
    cout&lt;&lt;(ans+1)*4&lt;&lt;&quot;\n&quot;;
&#125;
</code></pre>
]]></content>
  </entry>
  <entry>
    <title>数位DP</title>
    <url>/2024/09/12/%E6%95%B0%E4%BD%8DDP/</url>
    <content><![CDATA[<h2 id="ZJOI2010-数字计数"><a href="#ZJOI2010-数字计数" class="headerlink" title="[ZJOI2010] 数字计数"></a>[ZJOI2010] 数字计数</h2><h3 id="题目描述"><a href="#题目描述" class="headerlink" title="题目描述"></a>题目描述</h3><p>给定两个正整数 $a$ 和 $b$，求在 $[a,b]$ 中的所有整数里，每个数码 $0\sim 9$ 分别出现了多少次。</p>
<span id="more"></span>
<h3 id="输入格式"><a href="#输入格式" class="headerlink" title="输入格式"></a>输入格式</h3><p>一行两个整数 $a,b$。</p>
<h3 id="输出格式"><a href="#输出格式" class="headerlink" title="输出格式"></a>输出格式</h3><p>一行十个整数，分别表示 $0\sim 9$ 在 $[a,b]$ 中出现了多少次。</p>
<h3 id="样例输入"><a href="#样例输入" class="headerlink" title="样例输入"></a>样例输入</h3><pre><code class="lang-text">1 99
</code></pre>
<h3 id="样例输出"><a href="#样例输出" class="headerlink" title="样例输出"></a>样例输出</h3><pre><code class="lang-text">9 20 20 20 20 20 20 20 20 20
</code></pre>
<h3 id="数据范围"><a href="#数据范围" class="headerlink" title="数据范围"></a>数据范围</h3><ul>
<li>对于 $30\%$ 的数据，保证 $a \le b \le 10^6$。</li>
<li>对于 $100\%$ 的数据，保证 $1 \le a \le b \le 10^{12}$。</li>
</ul>
<h3 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h3><p>设 $dp_i$ 表示在不考虑前导零时，满 $i$ 位范围内某一种数码出现的次数。</p>
<p>对前 $i$ 位进行统计时，可以分两类讨论：</p>
<ol>
<li>前导零合法时，$0\sim 9$ 的数量相同，可以由 $dp_{i-1}$ 递推得到。</li>
<li>前导零不合法时，需要单独处理最高位为 $0$ 的情况。</li>
</ol>
<p>这类题的核心是把 $[1,n]$ 的统计函数写出来，再用答案 $calc(b)-calc(a-1)$ 得到区间结果。</p>
]]></content>
  </entry>
  <entry>
    <title>2024“钉耙编程”中国大学生算法设计超级联赛(4)</title>
    <url>/2024/07/28/2024%E2%80%9C%E9%92%89%E8%80%99%E7%BC%96%E7%A8%8B%E2%80%9D%E4%B8%AD%E5%9B%BD%E5%A4%A7%E5%AD%A6%E7%94%9F%E7%AE%97%E6%B3%95%E8%AE%BE%E8%AE%A1%E8%B6%85%E7%BA%A7%E8%81%94%E8%B5%9B-4/</url>
    <content><![CDATA[<h2 id="序列更新"><a href="#序列更新" class="headerlink" title="序列更新"></a>序列更新</h2><p>给定两个长度为 $n$ 的序列：</p>
<script type="math/tex; mode=display">
a_0,a_1,\cdots,a_{n-1}</script><p>和</p>
<script type="math/tex; mode=display">
b_0,b_1,\cdots,b_{n-1}</script><p>需要依次执行 $q$ 次操作。每次操作给出一个整数 $k$，按下标关系维护序列更新。</p>
<span id="more"></span>
<h3 id="输入"><a href="#输入" class="headerlink" title="输入"></a>输入</h3><p>第一行包含一个正整数 $T$，表示测试数据组数，满足 $1 \le T \le 2$。</p>
<p>每组数据第一行包含两个正整数 $n,q$，分别表示序列长度和操作次数，满足 $1 \le n,q \le 250000$。</p>
<p>第二行包含 $n$ 个正整数 $a<em>0,a_1,\cdots,a</em>{n-1}$，满足 $1 \le a_i \le 10^9$。</p>
<p>第三行包含 $n$ 个正整数 $b<em>0,b_1,\cdots,b</em>{n-1}$，满足 $1 \le b_i \le 10^9$。</p>
<p>接下来 $q$ 行，每行包含一个整数 $k$。</p>
<h3 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h3><p>按 $k$ 的大小分类处理：</p>
<ul>
<li>当 $k$ 较大时，直接暴力更新。</li>
<li>当 $k$ 较小时，预处理对应的余数类，用 $b<em>i$ 更新 $a</em>{(i-k) \bmod k}$。</li>
</ul>
<p>设当前大于阈值 $S$ 的元素数量为 $x$。如果连续若干次操作都没有命中这些元素，其概率可以近似写成 $(1-\frac{x}{n})$ 的幂次，因此期望次数为：</p>
<script type="math/tex; mode=display">
\frac{x}{n}\sum_{i\ge 0}(i+1)\left(1-\frac{x}{n}\right)^i \approx \frac{n}{x}</script><p>令 $p=\frac{x}{n}$，则：</p>
<script type="math/tex; mode=display">
ans=p\sum_{i\ge 0}(i+1)(1-p)^i</script><p>由等比数列求和可得：</p>
<script type="math/tex; mode=display">
ans=\frac{1}{p}=\frac{n}{x}</script><p>在实现上，可以用根号分治平衡两部分复杂度。总复杂度近似为：</p>
<script type="math/tex; mode=display">
O\left(nx+\frac{n^2}{x}\right)</script><p>当 $nx=\frac{n^2}{x}$ 时，取 $x=\sqrt n$ 最优，复杂度约为 $O(n\sqrt n)$。</p>
<h2 id="最优-K-子段"><a href="#最优-K-子段" class="headerlink" title="最优 K 子段"></a>最优 K 子段</h2><h3 id="分析-1"><a href="#分析-1" class="headerlink" title="分析"></a>分析</h3><p>首先二分答案。对于每个候选值 $mid$，在 <code>check</code> 中贪心维护最小值不小于 $mid$ 的最多子段数量。</p>
<p>从左端点开始枚举，通过维护一个前缀集合 <code>set</code>，保证以位置 $i$ 结尾的子段满足：</p>
<script type="math/tex; mode=display">
pre_i-set.pre \ge x</script><p>接着需要估计：对每个 $i$，暴力枚举 <code>set</code> 中满足 $i-set.pos$ 为质数的元素，复杂度是多少。</p>
<p>因为 $1\sim n$ 中质数个数约为 $\frac{n}{\ln n}$，随机命中质数间隔的期望为 $O(\log n)$，所以单次枚举的期望复杂度为 $O(\log n)$。</p>
<p>最终总时间复杂度约为：</p>
<script type="math/tex; mode=display">
O(n\log^2 n)</script><h3 id="序列更新代码"><a href="#序列更新代码" class="headerlink" title="序列更新代码"></a>序列更新代码</h3><pre><code class="lang-cpp">#include
using namespace std;
#define ll long long
#define pb push_back
#define int long long
#define PII pair
#define PI acos(-1)
#define s second
#define f first
#define Bint __int128
#define all(x) x.begin(),x.end()
#define isdigit(x) ((x) &gt;= &#39;0&#39; &amp;&amp; (x) n)x-=n;
&#125;
inline void modify(int x,int y)&#123;
    fix(x);
    fix(y);
    if(a[x]&gt;=b[y])return ;
    sum+=b[y]-a[x];
    a[x]=b[y];
&#125;
void solve()&#123;
    ca=0,cb=0;
    cin&gt;&gt;n&gt;&gt;q;
    sum=0;
    for(int i=1;i&gt;a[i];
    for(int i=1;i&gt;b[i],c[i]=b[i];
    int s=sqrt(n);
    s=max(s,(int)1);
    sort(c+1,c+1+n);
    for(int i=1;is)B[cb++]=i;
    &#125;
    while(q--)&#123;
        int k;cin&gt;&gt;k;
        for(int i=0;is)swap(A[i],A[--ca]);else i++;
        &#125;
        for(int i=0;i&gt;T;
    while(T--)&#123;
        solve();
    &#125;
    return 0;
&#125;
</code></pre>
<h3 id="最优-K-子段代码"><a href="#最优-K-子段代码" class="headerlink" title="最优 K 子段代码"></a>最优 K 子段代码</h3><pre><code class="lang-cpp">#include
using namespace std;
#define ll long long
#define pb push_back
#define int long long
#define PII pair
#define PI acos(-1)
#define s second
#define f first
#define Bint __int128
#define all(x) x.begin(),x.end()
#define isdigit(x) ((x) &gt;= &#39;0&#39; &amp;&amp; (x) b;
void get_primes(int n)
&#123;
    st[1]=1;
    for (int i = 2; i f&gt;=x)&#123;
            bool f=0;
            for(auto &amp;[p,pos]:b)&#123;
                if(s-p&gt;=x&amp;&amp;!st[i-pos])&#123;
                    f=1;
                    break;
                &#125;
                else if(s-p&gt;n&gt;&gt;k;
    for(int i=1;i&gt;a[i],pre[i]=a[i]+pre[i-1];

    int l=0,r=0;
    for(int i=1;i=0)r+=a[i];
    else l+=a[i];
    while(l&gt;1;
        if(check(mid))l=mid;
        else r=mid-1;
    &#125;
    if(check(l))
    cout&gt;T;
    while(T--)&#123;
        solve();
    &#125;
    return 0;
&#125;
</code></pre>
]]></content>
      <categories>
        <category>比赛</category>
      </categories>
      <tags>
        <tag>期望</tag>
        <tag>概率论</tag>
        <tag>根号思想</tag>
      </tags>
  </entry>
  <entry>
    <title>2024暑假集训第一周总结</title>
    <url>/2024/07/20/2024%E6%9A%91%E5%81%87%E9%9B%86%E8%AE%AD%E7%AC%AC%E4%B8%80%E5%91%A8%E6%80%BB%E7%BB%93/</url>
    <content><![CDATA[<p><em>迟来的第一周总结……</em></p>
<span id="more"></span>
<p>第一周一共打了四场比赛。整体感受是：还有很多知识点没学，或者学得不够深。八月之前应该会持续补新知识，八月之后再重点加深理解和熟练度。比赛前期大家能过的题差距不大，后期更多取决于知识点覆盖和掌握程度。</p>
<h2 id="牛客多校一"><a href="#牛客多校一" class="headerlink" title="牛客多校一"></a>牛客多校一</h2><p>这一场补了三个题。</p>
<p><a href="https://ac.nowcoder.com/acm/contest/81596/A">A - A Bit Common</a></p>
<h3 id="题意"><a href="#题意" class="headerlink" title="题意"></a>题意</h3><p>给定 $n$、$m$、$q$，其中 $n,m \le 5000$，$q \le 10^9$。从 $[0,2^m)$ 中取数构成长度为 $n$ 的序列，要求序列中存在一个子序列，其按位与结果为 $1$。求方案数并对 $q$ 取模。</p>
<h3 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h3><p>由于 $n,m$ 都是 $5000$，时间复杂度需要控制在 $O(n^2)$ 附近，因此可以考虑枚举子序列的长度。</p>
<p>假设子序列长度为 $k$，则位置选择有 $C(n,k)$ 种。一个数的按位与为 $1$ 时，最低位必须为 $1$；多个数按位与为 $1$ 时，它们最低位都为 $1$，其他位不能同时全为 $1$。</p>
<p>剩下的 $n-k$ 个数可以任意选择，因此得到公式：</p>
<script type="math/tex; mode=display">
\sum_{k=2}^{n} C(n,k)(2^k-1)^{m-1}2^{(m-1)(n-k)}</script><p>组合数用递推式预处理，枚举序列计算答案，整体时间复杂度约为 $O(n^2)$。这题本身不算难，主要还是组合计数推式子的训练不够。</p>
<p><a href="https://ac.nowcoder.com/acm/contest/81596/B">B - A Bit More Common</a></p>
<h3 id="题意-1"><a href="#题意-1" class="headerlink" title="题意"></a>题意</h3><p>题意和范围与 A 题类似，区别是要求序列中存在两个按位与结果为 $1$ 的子序列，计算满足条件的方案数。</p>
<h3 id="分析-1"><a href="#分析-1" class="headerlink" title="分析"></a>分析</h3><p>这题可以沿用 A 题的计数思路，但需要额外处理两个子序列之间的重叠关系。相比 A 题，难点不在组合数本身，而在于如何避免把同一组序列重复计入多个子序列条件中。</p>
]]></content>
  </entry>
  <entry>
    <title>费马小定理&amp;欧拉定理&amp;扩展欧拉定理</title>
    <url>/2024/06/10/%E8%B4%B9%E9%A9%AC%E5%B0%8F%E5%AE%9A%E7%90%86-%E6%AC%A7%E6%8B%89%E5%AE%9A%E7%90%86-%E6%89%A9%E5%B1%95%E6%AC%A7%E6%8B%89%E5%AE%9A%E7%90%86/</url>
    <content><![CDATA[<h2 id="费马小定理"><a href="#费马小定理" class="headerlink" title="费马小定理"></a>费马小定理</h2><p>当 $p$ 是质数，且 $a$ 与 $p$ 互质时，有：</p>
<span id="more"></span>
<script type="math/tex; mode=display">
a^{p-1} \equiv 1 \pmod p</script><p>因此在模质数的指数问题中，可以把指数对 $p-1$ 取模后再计算。</p>
<h2 id="欧拉定理"><a href="#欧拉定理" class="headerlink" title="欧拉定理"></a>欧拉定理</h2><p>当 $a$ 与 $m$ 互质时，有：</p>
<script type="math/tex; mode=display">
a^{\varphi(m)} \equiv 1 \pmod m</script><p>这里 $\varphi(m)$ 表示 $1\sim m$ 中与 $m$ 互质的整数个数。费马小定理可以看作欧拉定理在 $m$ 为质数时的特例。</p>
<h2 id="扩展欧拉定理"><a href="#扩展欧拉定理" class="headerlink" title="扩展欧拉定理"></a>扩展欧拉定理</h2><p>当指数 $b$ 很大，且 $a$ 与 $m$ 不一定互质时，常用下面的降幂形式：</p>
<script type="math/tex; mode=display">
a^b \equiv a^{b \bmod \varphi(m) + \varphi(m)} \pmod m \quad (b \ge \varphi(m))</script><p>如果 $b &lt; \varphi(m)$，直接使用原指数即可。</p>
<h2 id="使用场景"><a href="#使用场景" class="headerlink" title="使用场景"></a>使用场景</h2><ul>
<li>模数是质数，并且底数与模数互质：优先用费马小定理。</li>
<li>模数不一定是质数，但底数与模数互质：使用欧拉定理。</li>
<li>指数特别大，且互质条件不确定：使用扩展欧拉定理。</li>
</ul>
]]></content>
      <categories>
        <category>欧拉定理</category>
      </categories>
      <tags>
        <tag>数论</tag>
        <tag>欧拉定理</tag>
        <tag>费马小定理</tag>
      </tags>
  </entry>
  <entry>
    <title>强连通分量(scc缩点/tarjan)</title>
    <url>/2024/06/09/%E5%BC%BA%E8%BF%9E%E9%80%9A%E5%88%86%E9%87%8F-scc%E7%BC%A9%E7%82%B9-tarjan/</url>
    <content><![CDATA[<p>学习了一下强连通分量，这里整理 Tarjan 求 SCC 和缩点的基本思路。</p>
<span id="more"></span>
<p>强连通分量指的是：在一个有向图中，任意两点之间都可以相互到达的最大点集。</p>
<p>缩点可以理解为“把环缩成点”。把每个强连通分量压成一个新点后，原图会变成一个 <code>DAG</code>（有向无环图），后续就可以在 DAG 上做拓扑、DP 等处理。</p>
<h2 id="Tarjan-求-SCC"><a href="#Tarjan-求-SCC" class="headerlink" title="Tarjan 求 SCC"></a>Tarjan 求 SCC</h2><p>Tarjan 求强连通分量的时间复杂度为：</p>
<script type="math/tex; mode=display">
O(n+m)</script><p>单纯判断是否有环一般可以用 DFS。在搜索过程中，一条边大致会遇到三种情况：</p>
<ol>
<li>指向一个还没有搜索过的点。</li>
<li>指向一个搜索过的点，但这个点不是当前 DFS 链上的祖先。</li>
<li>指向一个搜索过的点，并且这个点是当前 DFS 链上的祖先。</li>
</ol>
<p>下图描述了这三种情况：黑色边为 <code>1</code>，绿色边为 <code>2</code>，蓝色边为 <code>3</code>。</p>
<p><img src="https://lifexoryoung.cn/images/posts/scc-tarjan/dfs-edge-types.webp?v=202607071306" alt="DFS 过程中三类边的示意图"></p>
<p>只有第三类边会形成环。形成环的点属于同一个强连通分量，其余不能互相到达的点会各自成为单点强连通分量。以上图为例，强连通分量可以表示为 <code>&#123;1&#125;</code>、<code>&#123;2, 3&#125;</code>、<code>&#123;4&#125;</code>、<code>&#123;5&#125;</code>。</p>
<p>实际图中可能存在多个环互相嵌套，所以不能在第一次发现环时就立刻缩点。Tarjan 的做法是：</p>
<ul>
<li>用 <code>dfn[u]</code> 记录节点 $u$ 第一次被访问的时间戳。</li>
<li>用 <code>low[u]</code> 记录节点 $u$ 能回到的最早时间戳。</li>
<li>用一个栈维护当前 DFS 链上的点。</li>
</ul>
<p>当某个点满足 <code>dfn[u] == low[u]</code> 时，说明它是当前强连通分量中最早被访问的点。此时从栈顶不断弹出节点，直到弹出 <code>u</code>，这些点就构成一个完整的强连通分量。</p>
<h3 id="模板"><a href="#模板" class="headerlink" title="模板"></a>模板</h3><pre><code class="lang-cpp">int dfn[N], low[N], scc[N], sz[N];
int timer, scc_cnt;
stack&lt;int&gt; st;
vector&lt;int&gt; e[N];
bool in_stack[N];

void tarjan(int u) &#123;
    dfn[u] = low[u] = ++timer;
    st.push(u);
    in_stack[u] = true;

    for (int v : e[u]) &#123;
        if (!dfn[v]) &#123;
            tarjan(v);
            low[u] = min(low[u], low[v]);
        &#125; else if (in_stack[v]) &#123;
            low[u] = min(low[u], dfn[v]);
        &#125;
    &#125;

    if (dfn[u] == low[u]) &#123;
        ++scc_cnt;
        while (true) &#123;
            int x = st.top();
            st.pop();
            in_stack[x] = false;
            scc[x] = scc_cnt;
            sz[scc_cnt]++;
            if (x == u) break;
        &#125;
    &#125;
&#125;
</code></pre>
<h2 id="例题：P3387【模板】缩点"><a href="#例题：P3387【模板】缩点" class="headerlink" title="例题：P3387【模板】缩点"></a>例题：P3387【模板】缩点</h2><h3 id="题目描述"><a href="#题目描述" class="headerlink" title="题目描述"></a>题目描述</h3><p>给定一个 $n$ 个点、$m$ 条边的有向图，每个点有一个权值。求一条路径，使路径经过的点权值之和最大。重复经过同一个点时，点权只计算一次。</p>
<h3 id="输入格式"><a href="#输入格式" class="headerlink" title="输入格式"></a>输入格式</h3><p>第一行两个正整数 $n,m$。</p>
<p>第二行 $n$ 个整数，其中第 $i$ 个数 $a_i$ 表示点 $i$ 的点权。</p>
<p>接下来 $m$ 行，每行两个整数 $u,v$，表示一条 $u\to v$ 的有向边。</p>
<h3 id="输出格式"><a href="#输出格式" class="headerlink" title="输出格式"></a>输出格式</h3><p>输出最大的点权之和。</p>
<h3 id="思路"><a href="#思路" class="headerlink" title="思路"></a>思路</h3><p>因为原图是有向图，可能存在环。路径可以多次经过边或点，因此同一个强连通分量中的所有点权都可以被取到。</p>
<p>先用 Tarjan 求 SCC，并把每个强连通分量缩成一个点。缩点后得到 DAG，新点权值为该 SCC 内所有点权之和。</p>
<p>问题就变成：在 DAG 上求点权最长路径。可以按拓扑序做 DP：</p>
<script type="math/tex; mode=display">
dp[v]=\max(dp[v], dp[u]+w_v)</script><p>最终答案就是所有 $dp$ 值的最大值。</p>
<h3 id="缩点代码"><a href="#缩点代码" class="headerlink" title="缩点代码"></a>缩点代码</h3><pre><code class="lang-cpp">int dfn[N], low[N],cnt,sz[N],scc[N],sc,d[N],dp[N],ans,w[N];
stack&lt;int&gt; st;
vector&lt;int&gt; e[N], ex[N];
bool vis[N];
queue&lt;int&gt; q;
void tarjan(int u)&#123;
    dfn[u]=low[u]=++cnt;
    st.push(u),vis[u]=true;
    for (auto &amp;to : e[u])&#123;
        if(!dfn[to])&#123;
            tarjan(to);
            low[u]=min(low[u],low[to]);
        &#125;
        else if(vis[to])&#123;
            low[u]=min(low[u],dfn[to]);
        &#125;
    &#125;
    if(dfn[u]==low[u])&#123;
        sc++;
        while(st.top()!=u)&#123;
            sz[sc]+=w[st.top()];
            scc[st.top()]=sc;
            vis[st.top()]=0;
            st.pop();
        &#125;
        sz[sc]+=w[st.top()];
        scc[st.top()]=sc;
        vis[st.top()]=0;
        st.pop();
    &#125;
&#125;
void topu()&#123;
    for(int i=1;i&gt;n&gt;&gt;m;
    for(int i=1;i&gt;w[i];
    for(int i=1;i&gt;u&gt;&gt;v;
        e[u].pb(v);
    &#125;

    for(int i=1;ist;
vector&lt;int&gt; e[N], ex[N];
bool vis[N];
queue&lt;int&gt; q;
void tarjan(int u)&#123;
    dfn[u]=low[u]=++cnt;
    st.push(u),vis[u]=true;
    for (auto &amp;to : e[u])&#123;
        if(!dfn[to])&#123;
            tarjan(to);
            low[u]=min(low[u],low[to]);
        &#125;
        else if(vis[to])&#123;
            low[u]=min(low[u],dfn[to]);
        &#125;
    &#125;
    if(dfn[u]==low[u])&#123;
        sc++;
        while(st.top()!=u)&#123;
            scc[st.top()]=sc;
            vis[st.top()]=0;
            st.pop();
        &#125;
        scc[st.top()]=sc;
        vis[st.top()]=0;
        st.pop();
    &#125;
&#125;
void solve()&#123;
    int n;cin&gt;&gt;n;
    for(int i=1;i&gt;x,x)&#123;
            e[i].pb(x);
        &#125;
    &#125;

    for(int i=1;i&lt;=n;i++)
    if(!dfn[i])tarjan(i);

    int ans1=0,ans2=0;
    for(int i=1;i&lt;=n;i++)&#123;
        int u=scc[i];
        for(auto &amp;to:e[i])&#123;
            int v=scc[to];
            if(u==v)continue;
            d[v]++,ud[u]++;
        &#125;
    &#125;
    for(int i=1;i&lt;=sc;i++)
    &#123;
        if(!d[i])ans1++;
        if(!ud[i])ans2++;
    &#125;
    cout&lt;&lt;ans1&lt;&lt;&quot;\n&quot;;
    if(sc==1)
    cout&lt;&lt;0&lt;&lt;&quot;\n&quot;;
    else cout&lt;&lt;max(ans1,ans2)&lt;&lt;&quot;\n&quot;;
&#125;
</code></pre>
]]></content>
      <categories>
        <category>笔记</category>
      </categories>
      <tags>
        <tag>dfs</tag>
        <tag>强连通分量</tag>
        <tag>tarjan</tag>
        <tag>拓扑</tag>
      </tags>
  </entry>
  <entry>
    <title>lca与树上（路径交、并，直径）问题</title>
    <url>/2024/05/26/lca%E4%B8%8E%E6%A0%91%E4%B8%8A%EF%BC%88%E8%B7%AF%E5%BE%84%E4%BA%A4%E3%80%81%E5%B9%B6%EF%BC%8C%E7%9B%B4%E5%BE%84%EF%BC%89%E9%97%AE%E9%A2%98/</url>
    <content><![CDATA[<p>最近的一些比赛中经常用到树上 $LCA$ 的常见模型，这里整理一下几种做法和应用场景。</p>
<span id="more"></span>
<h2 id="LCA-常见求法"><a href="#LCA-常见求法" class="headerlink" title="LCA 常见求法"></a>LCA 常见求法</h2><h3 id="倍增-LCA"><a href="#倍增-LCA" class="headerlink" title="倍增 LCA"></a>倍增 LCA</h3><p>预处理复杂度为 $O(n\log n)$，单次查询复杂度为 $O(\log n)$。</p>
<p>基本思路是先用 BFS 或 DFS 预处理每个节点深度，并用倍增数组 $f[u][i]$ 表示节点 $u$ 向上跳 $2^i$ 步后的祖先。查询时先把两个点跳到同一深度，再从高位到低位一起向上跳，最终得到最近公共祖先。</p>
<h3 id="Tarjan-离线-LCA"><a href="#Tarjan-离线-LCA" class="headerlink" title="Tarjan 离线 LCA"></a>Tarjan 离线 LCA</h3><p>总体复杂度为 $O(n+q)$，适合所有查询已知、可以离线处理的场景。</p>
<p>核心是 DFS 遍历树，并用并查集维护已经访问过的子树。当一个询问的另一个端点已经访问过时，就可以用并查集祖先得到答案。</p>
<h3 id="欧拉序-RMQ"><a href="#欧拉序-RMQ" class="headerlink" title="欧拉序 + RMQ"></a>欧拉序 + RMQ</h3><p>预处理复杂度为 $O(n\log n)$，单次查询复杂度为 $O(1)$。</p>
<p>做法是 DFS 处理欧拉序，记录每个节点第一次出现的位置，并用 ST 表维护欧拉序区间内深度最小的节点。两个点的 LCA 就是它们第一次出现位置之间深度最小的节点。</p>
<h3 id="树链剖分-LCA"><a href="#树链剖分-LCA" class="headerlink" title="树链剖分 LCA"></a>树链剖分 LCA</h3><p>预处理复杂度为 $O(n)$ 或 $O(n\log n)$，单次查询复杂度为 $O(\log n)$。</p>
<p>树链剖分更适合需要同时维护路径信息的题，比如路径修改、路径查询等。</p>
<h2 id="常见模型"><a href="#常见模型" class="headerlink" title="常见模型"></a>常见模型</h2><p>树上 $LCA$ 常见应用包括：</p>
<ol>
<li>动态维护树的直径。</li>
<li>判断两条树上路径是否相交。</li>
<li>统计树上路径的并。</li>
</ol>
<p>这些模型的关键通常是把路径 $(u,v)$ 拆成 $u\to LCA(u,v)$ 和 $v\to LCA(u,v)$ 两段，再用深度、距离或 DFS 序进行判断。</p>
<h3 id="倍增-LCA-代码"><a href="#倍增-LCA-代码" class="headerlink" title="倍增 LCA 代码"></a>倍增 LCA 代码</h3><pre><code class="lang-cpp">int n,depth[N],f[N][19];
vector&lt;int&gt; e[N];
void bfs(int root)&#123;
    memset(depth,0x3f,sizeof depth);
    depth[0]=0,depth[root]=1;//0为st表的哨兵
    queue&lt;int&gt; q;
    q.push(root);
    while(q.size())&#123;
        int ver=q.front();
        q.pop();
        for(auto &amp;to:e[ver])&#123;
            if(depth[to]&gt;depth[ver]+1)&#123;
                depth[to]=depth[ver]+1;
                f[to][0]=ver;
                for(int i=1;i=0;i--)&#123;
        if(depth[f[a][i]]=0;i--)&#123;
        if(f[a][i]!=f[b][i])
        a=f[a][i],b=f[b][i];
    &#125;
    return f[a][0];
&#125;
</code></pre>
<h3 id="欧拉序-RMQ-代码"><a href="#欧拉序-RMQ-代码" class="headerlink" title="欧拉序 RMQ 代码"></a>欧拉序 RMQ 代码</h3><pre><code class="lang-cpp">int n,q,root,depth[Ne[N];
void dfs(int u,int d,int fa)&#123;
    se[++tot]=u;
    id[u]=tot;
    depth[tot]=d;
    for (auto &amp;to : e[u])&#123;
        if(to==fa)continue;
        dfs(to,d+1,u);
        se[++tot]=u;
        depth[tot]=d;
    &#125;
&#125;
int lca(int l,int r)&#123;
    int k=Log[r-l+1];
    return depth[f[l][k]]&gt;n&gt;&gt;q&gt;&gt;root;
    for(int i=1;i&gt;u&gt;&gt;v;
        e[u].pb(v);
        e[v].pb(u);
    &#125;
    dfs(root,1,0);
    Log[1]=0,Log[2]=1;
    for(int i=3;i&gt;u&gt;&gt;v;
        int l=id[u],r=id[v];
        if(l&gt;r)swap(l,r);
    &#125;
&#125;
</code></pre>
]]></content>
      <categories>
        <category>笔记</category>
      </categories>
      <tags>
        <tag>图论</tag>
        <tag>树论</tag>
        <tag>LCA</tag>
        <tag>Tarjan</tag>
        <tag>st表</tag>
        <tag>数据结构</tag>
        <tag>并查集</tag>
        <tag>线段树</tag>
        <tag>dfs</tag>
        <tag>bfs</tag>
      </tags>
  </entry>
  <entry>
    <title>2023 Hubei Provincial Collegiate Programming Contest</title>
    <url>/2024/04/02/2023%20Hubei%20Provincial%20Collegiate%20Programming%20Contest/</url>
    <content><![CDATA[<p><a href="https://codeforces.com/gym/104337/problem/I">Problem I - Step</a></p>
<span id="more"></span>
<h3 id="题目描述"><a href="#题目描述" class="headerlink" title="题目描述"></a>题目描述</h3><p>给定 $n$ 个环，第 $i$ 个环的长度为 $p_i$。每个环的 $1$ 号位置一开始都有一匹小马，小马第 $k$ 天会移动 $k$ 步。</p>
<p>要求找到最早的一天 $m$，使得所有小马都回到各自环上的 $1$ 号位置。这里 $m$ 不能为 $0$。</p>
<h3 id="输入"><a href="#输入" class="headerlink" title="输入"></a>输入</h3><p>第一行包含一个正整数 $n$，表示环的数量，满足 $1 \le n \le 10^5$。</p>
<p>第二行包含 $n$ 个正整数 $p_i$，表示每个环的长度，满足 $1 \le p_i \le 10^7$。</p>
<p>保证 $p_1,p_2,\ldots,p_n$ 的最小公倍数不超过 $10^{18}$。</p>
<h3 id="输出"><a href="#输出" class="headerlink" title="输出"></a>输出</h3><p>输出一个正整数，表示所有小马同时回到位置 $1$ 的最早日期。</p>
<h3 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h3><p>第 $m$ 天结束时，小马总共移动的步数为：</p>
<script type="math/tex; mode=display">
1+2+\cdots+m=\frac{m(m+1)}{2}</script><p>对于每个环，都需要满足：</p>
<script type="math/tex; mode=display">
\frac{m(m+1)}{2} \equiv 0 \pmod {p_i}</script><p>因此可以先把所有 $p_i$ 合并成一个模数条件，再根据奇偶性和整除关系处理 $m$ 与 $m+1$。这类题的关键是把“每天移动步数递增”转化为三角数取模问题。</p>
]]></content>
      <categories>
        <category>补题</category>
      </categories>
      <tags>
        <tag>扩展欧几里得</tag>
      </tags>
  </entry>
  <entry>
    <title>2024 ICPC National Invitational Collegiate Programming Contest, Wuhan Site（2024武汉邀请赛）</title>
    <url>/2024/04/02/2024%20ICPC%20National%20Invitational%20Collegiate%20Programming%20Contest,%20Wuhan%20Site%EF%BC%882024%E6%AD%A6%E6%B1%89%E9%82%80%E8%AF%B7%E8%B5%9B%EF%BC%89/</url>
    <content><![CDATA[<h2 id="Problem-B-Countless-Me"><a href="#Problem-B-Countless-Me" class="headerlink" title="Problem B - Countless Me"></a>Problem B - Countless Me</h2><h3 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h3><p>假设最优情况下，目标序列的每个值分别为：</p>
<script type="math/tex; mode=display">
x_1,x_2,\cdots,x_n</script><p>把原序列变成任意目标序列时，每个位置最多只需要一次操作，因此整体可以看作“构造一个更优的目标序列”。</p>
<span id="more"></span>
<p>由于题目和位运算有关，可以从高位到低位贪心判断当前位是否可以保留。设当前所有值的总和为 $now$，每一位从高到低尝试加入答案。</p>
<p>如果当前位可以通过剩余总和分配出来，就保留这一位；否则跳过。这样可以保证高位优先最大化。</p>
<p>整体思路是：</p>
<ol>
<li>统计所有数的总和。</li>
<li>从高位到低位枚举答案的每一位。</li>
<li>判断当前剩余总和能否支持这一位的构造。</li>
<li>如果可以，就把这一位加入答案，并扣掉对应消耗。</li>
</ol>
<p>这种做法本质上是按位贪心，高位优先保证最终答案最大。</p>
<h3 id="代码"><a href="#代码" class="headerlink" title="代码"></a>代码</h3><pre><code class="lang-cpp">    for(int i=1;i&gt;x;
        res+=x;
    &#125;
    int ans=0;
    for(int i=30;i&gt;=0;i--)&#123;
        int ver=((1llver)ans|=(1ll&lt;&lt;i),res-=min(n,res/(1ll&lt;&lt;i))*(1ll&lt;&lt;i);
    &#125;
    cout&lt;&lt;ans&lt;&lt;&quot;\n&quot;;
&#125;
</code></pre>
]]></content>
      <categories>
        <category>补题</category>
      </categories>
      <tags>
        <tag>扩展欧几里得</tag>
      </tags>
  </entry>
  <entry>
    <title>Codeforces Round 942 (Div. 2)</title>
    <url>/2024/04/02/Codeforces%20Round%20942%20(Div.%202)/</url>
    <content><![CDATA[<h2 id="D2-Reverse-Card-Hard-Version"><a href="#D2-Reverse-Card-Hard-Version" class="headerlink" title="D2. Reverse Card (Hard Version)"></a>D2. Reverse Card (Hard Version)</h2><h3 id="题意"><a href="#题意" class="headerlink" title="题意"></a>题意</h3><p>给定两个正整数 $n,m$，计算满足条件的有序数对 $(a,b)$ 的数量：</p>
<span id="more"></span>
<ul>
<li>$1 \le a \le n$，$1 \le b \le m$。</li>
<li>$b\cdot \gcd(a,b)$ 是 $a+b$ 的倍数。</li>
</ul>
<h3 id="输入"><a href="#输入" class="headerlink" title="输入"></a>输入</h3><p>每个测试包含多个测试用例。第一行包含测试用例数量 $t$，满足 $1 \le t \le 10^4$。</p>
<p>每个测试用例包含两个整数 $n,m$，满足 $1 \le n,m \le 2\cdot 10^6$。</p>
<p>所有测试用例中 $n$ 和 $m$ 的总和不超过 $2\cdot 10^6$。</p>
<h3 id="输出"><a href="#输出" class="headerlink" title="输出"></a>输出</h3><p>对每个测试用例输出一个整数，表示有效数对数量。</p>
<h3 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h3><p>设 $d=\gcd(a,b)$，并令：</p>
<script type="math/tex; mode=display">
a=pd, \quad b=qd</script><p>其中 $\gcd(p,q)=1$。原条件可以写成：</p>
<script type="math/tex; mode=display">
b\cdot\gcd(a,b)=k(a+b)</script><p>代入后得到：</p>
<script type="math/tex; mode=display">
qd^2 = k(p+q)d</script><p>也就是：</p>
<script type="math/tex; mode=display">
qd = k(p+q)</script><p>因为 $\gcd(p,q)=1$，所以 $\gcd(p+q,q)=1$，从而可以推出 $p+q$ 需要整除 $d$。</p>
<p>枚举互质的 $p,q$，它们对答案的贡献为：</p>
<script type="math/tex; mode=display">
\left\lfloor \frac{\min(\lfloor n/p\rfloor,\lfloor m/q\rfloor)}{p+q} \right\rfloor</script><p>因此可以枚举 $p\le \sqrt n$、$q\le \sqrt m$，并用 $\gcd(p,q)=1$ 判断是否有效。</p>
<h3 id="代码"><a href="#代码" class="headerlink" title="代码"></a>代码</h3><pre><code class="lang-cpp">void solve() &#123;
    int n, m;
    cin &gt;&gt; n &gt;&gt; m;
    int ans = 0;
    for (int p = 1; p &lt;= n / p; p++) &#123;
        for (int q = 1; q &lt;= m / q; q++) &#123;
            if (__gcd(p, q) == 1) &#123;
                ans += min(n / p, m / q) / (p + q);
            &#125;
        &#125;
    &#125;
    cout &lt;&lt; ans &lt;&lt; &quot;\n&quot;;
&#125;
</code></pre>
]]></content>
      <categories>
        <category>补题</category>
      </categories>
      <tags>
        <tag>扩展欧几里得</tag>
      </tags>
  </entry>
  <entry>
    <title>2023icpc南京站补题</title>
    <url>/2024/03/31/2023icpc%E5%8D%97%E4%BA%AC%E7%AB%99%E8%A1%A5%E9%A2%98/</url>
    <content><![CDATA[<h2 id="思路简记"><a href="#思路简记" class="headerlink" title="思路简记"></a>思路简记</h2><p>建图后，可以枚举每个点作为起点跑 Dijkstra。如果最短路过程中再次回到起点，就得到经过该点的一个最小环候选值；再结合题目给定的阈值判断答案。</p>
<span id="more"></span>
<h3 id="代码"><a href="#代码" class="headerlink" title="代码"></a>代码</h3><pre><code class="lang-cpp">#include &lt;bits/stdc++.h&gt;
using namespace std;

const int N = 2010;
const int INF = 0x3f3f3f3f;

vector&lt;pair&lt;int, int&gt;&gt; e[N];
int n, m, c, minn, dist[N];
bool st[N];

void Dijkstra(int u) &#123;
    minn = INF;
    for (int i = 1; i &lt;= n; i++) &#123;
        dist[i] = INF;
        st[i] = false;
    &#125;

    priority_queue&lt;pair&lt;int, int&gt;, vector&lt;pair&lt;int, int&gt;&gt;, greater&lt;pair&lt;int, int&gt;&gt;&gt; q;
    dist[u] = 0;
    q.push(&#123;dist[u], u&#125;);

    while (!q.empty()) &#123;
        auto [dis, ver] = q.top();
        q.pop();
        if (st[ver]) continue;
        st[ver] = true;

        for (auto [v, w] : e[ver]) &#123;
            if (v == u) minn = min(minn, dis + w);
            if (st[v]) continue;
            if (dist[v] &gt; dis + w) &#123;
                dist[v] = dis + w;
                q.push(&#123;dist[v], v&#125;);
            &#125;
        &#125;
    &#125;
&#125;

void solve() &#123;
    cin &gt;&gt; n &gt;&gt; m &gt;&gt; c;
    int ans = 0;

    for (int i = 1; i &lt;= m; i++) &#123;
        int u, v, z;
        cin &gt;&gt; u &gt;&gt; v &gt;&gt; z;
        e[u].push_back(&#123;v, z&#125;);
        if (z &lt;= c) ans = 1;
    &#125;

    for (int i = 1; i &lt;= n; i++) &#123;
        Dijkstra(i);
        if (minn &lt;= c) &#123;
            ans = 2;
            break;
        &#125;
    &#125;

    cout &lt;&lt; ans &lt;&lt; &quot;\n&quot;;
&#125;
</code></pre>
]]></content>
      <categories>
        <category>补题</category>
      </categories>
      <tags>
        <tag>图论</tag>
        <tag>最小环</tag>
        <tag>最短路</tag>
        <tag>Dijkstra</tag>
      </tags>
  </entry>
  <entry>
    <title>D.Birthday Gift</title>
    <url>/2024/03/31/Codeforces%20Round%20936%20(Div.%202)D.%20Birthday%20Gift/</url>
    <content><![CDATA[<h2 id="Problem-1946D-Birthday-Gift"><a href="#Problem-1946D-Birthday-Gift" class="headerlink" title="Problem 1946D - Birthday Gift"></a>Problem 1946D - Birthday Gift</h2><h3 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h3><p>对于区间位运算，比较自然的做法是按位拆分，并从高位到低位分析 $x$ 的每一位。</p>
<span id="more"></span>
<ol>
<li><p>如果第 $i$ 位为 $0$，并且数组中这一位为 $1$ 的元素个数为奇数，那么无论如何分段，都会有一段的异或结果在这一位为 $1$，从而导致整体大于 $x$。</p>
</li>
<li><p>如果第 $i$ 位为 $0$，并且这一位为 $1$ 的元素个数为偶数，则需要尽量就近两两合并，保证这一位异或为 $0$，同时让分组数量尽可能多。</p>
</li>
<li><p>如果第 $i$ 位为 $1$，并且这一位为 $1$ 的元素个数为奇数，则当前位不需要强制处理，可以继续向低位传递。</p>
</li>
<li><p>如果第 $i$ 位为 $1$，并且这一位为 $1$ 的元素个数为偶数，则两两合并可以保证这一位异或为 $0$，并得到一个候选答案；但不合并也可能在低位获得更多分组，所以也要把“不处理”的情况继续向后传递。</p>
</li>
</ol>
<p>由于第 4 种情况里会保留与 $x$ 当前前缀相等的状态，可以考虑对 $x$ 加一后再处理，这样在传递到低位时能统一处理边界。</p>
]]></content>
      <categories>
        <category>补题</category>
      </categories>
      <tags>
        <tag>位运算</tag>
      </tags>
  </entry>
  <entry>
    <title>博客创建的流程</title>
    <url>/2024/03/31/%E5%8D%9A%E5%AE%A2%E5%88%9B%E5%BB%BA%E7%9A%84%E6%B5%81%E7%A8%8B/</url>
    <content><![CDATA[<p>记录一下 <code>$blog</code> 创建、换主题、部署和加速的大体流程。</p>
<span id="more"></span>
<p>最开始是跟着枫叶的教程，用 <code>Hexo + Next</code> 搭出了第一版博客。</p>
<ol>
<li><a href="https://zhuanlan.zhihu.com/p/102592286">从零开始搭建个人博客（超详细） - 知乎</a></li>
</ol>
<p>教程整体比较完整。如果某一步报错，可以先看评论区，很多细节问题已经有人补充过。</p>
<ol>
<li>在 <a href="https://hexo.io/themes/">Themes | Hexo</a> 里选择自己喜欢的主题，下载后放到 <code>$blog/themes</code> 目录，再在配置里切换主题。</li>
</ol>
<p>第一个入口是 <code>GitHub</code> 中的下载地址，点进去点击 <code>Download</code> 即可：</p>
<p><img src="https://lifexoryoung.cn/images/posts/blog-create/myblog1.webp?v=202607071306" alt="Hexo 主题下载入口"></p>
<p>第二个入口是主题的展示效果：</p>
<p><img src="https://lifexoryoung.cn/images/posts/blog-create/myblog2.webp?v=202607071306" alt="Hexo 主题展示效果"></p>
<p>一般来说，主题展示站首页的第一篇博客会介绍主题配置方式：</p>
<p><img src="https://lifexoryoung.cn/images/posts/blog-create/myblog3.webp?v=202607071306" alt="主题配置说明入口"></p>
<ol>
<li>常用工具记录。</li>
</ol>
<p><a href="https://try8.cn/tool/color/picker?eqid=f8d057810004bfbd0000000364759ea9">在线调色板</a>：用于获取颜色值，方便修改主题配色。</p>
<p><a href="https://www.likebizhi.com/tag/4k_page_1.html">高清 4K 壁纸</a>：可以作为博客背景图素材来源。</p>
<p><a href="https://fontawesome.com.cn/v5">FontAwesome 图标库</a>：用于博客图标配置。</p>
<p><a href="https://www.imhunk.com/cloudflare-tutorials/#title1">Cloudflare 免费版教程</a>：用于配置 CDN 加速。Cloudflare 里还有不少速度优化选项，可以后续按需打开。</p>
<p><img src="https://lifexoryoung.cn/images/posts/blog-create/myblog4.webp?v=202607071306" alt="Cloudflare 速度优化配置"></p>
<p><a href="https://www.erenship.com/posts/db37.html">Hexo 博客站点加速不完全指南</a>：介绍了 Vercel 等补充加速方案。</p>
<p><a href="https://pagespeed.web.dev/analysis/https-lifexoryoung-com-cn/os1aw9wyc4?form_factor=desktop">PageSpeed Insights</a>：用于测试页面加载速度，并查看具体优化建议。</p>
]]></content>
      <categories>
        <category>笔记</category>
      </categories>
      <tags>
        <tag>hexo</tag>
        <tag>next</tag>
        <tag>vercel</tag>
        <tag>cloudflare</tag>
        <tag>cdn</tag>
      </tags>
  </entry>
  <entry>
    <title>扩展欧几里得</title>
    <url>/2024/03/31/%E6%89%A9%E6%AC%A7/</url>
    <content><![CDATA[<p>扩展欧几里得用于求解形如 $ax+by=m$ 的不定方程，也常用于求逆元。</p>
<span id="more"></span>
<h2 id="裴蜀定理"><a href="#裴蜀定理" class="headerlink" title="裴蜀定理"></a>裴蜀定理</h2><p>对于任意整数 $a,b$，方程</p>
<script type="math/tex; mode=display">
ax+by=m</script><p>有整数解的充要条件是：</p>
<script type="math/tex; mode=display">
\gcd(a,b) \mid m</script><p>因此可以先求出 $ax+by=\gcd(a,b)$ 的一组解，再按比例扩展到一般的 $m$。</p>
<h2 id="递归推导"><a href="#递归推导" class="headerlink" title="递归推导"></a>递归推导</h2><p>因为：</p>
<script type="math/tex; mode=display">
\gcd(a,b)=\gcd(b,a\bmod b)</script><p>假设递归下一层已经求出：</p>
<script type="math/tex; mode=display">
bx_2+(a\bmod b)y_2=\gcd(a,b)</script><p>又因为：</p>
<script type="math/tex; mode=display">
a\bmod b=a-\left\lfloor \frac{a}{b}\right\rfloor b</script><p>代入可得：</p>
<script type="math/tex; mode=display">
bx_2+\left(a-\left\lfloor \frac{a}{b}\right\rfloor b\right)y_2=\gcd(a,b)</script><p>整理系数：</p>
<script type="math/tex; mode=display">
ay_2+b\left(x_2-\left\lfloor \frac{a}{b}\right\rfloor y_2\right)=\gcd(a,b)</script><p>所以当前层的一组解为：</p>
<script type="math/tex; mode=display">
x_1=y_2, \quad y_1=x_2-\left\lfloor \frac{a}{b}\right\rfloor y_2</script><p>递归终点是 $b=0$，此时：</p>
<script type="math/tex; mode=display">
a\cdot 1+b\cdot 0=a</script><p>从终点向上回代，就能得到最终的 $x,y$。</p>
<h2 id="求逆元"><a href="#求逆元" class="headerlink" title="求逆元"></a>求逆元</h2><p>若要求 $a$ 在模 $m$ 意义下的逆元，即：</p>
<script type="math/tex; mode=display">
ax \equiv 1 \pmod m</script><p>可以转化为：</p>
<script type="math/tex; mode=display">
ax+my=1</script><p>只要 $\gcd(a,m)=1$，就可以用扩展欧几里得求出 $x$，再把 $x$ 调整到 $[0,m)$ 范围内即可。</p>
]]></content>
      <categories>
        <category>笔记</category>
      </categories>
      <tags>
        <tag>扩展欧几里得</tag>
        <tag>数论</tag>
        <tag>逆元</tag>
      </tags>
  </entry>
  <entry>
    <title>最小环-Dijkstra-Floyd</title>
    <url>/2024/03/31/%E6%9C%80%E5%B0%8F%E7%8E%AF/</url>
    <content><![CDATA[<h2 id="2021-CCPC-桂林-E-Buy-and-Delete"><a href="#2021-CCPC-桂林-E-Buy-and-Delete" class="headerlink" title="2021 CCPC 桂林 E - Buy and Delete"></a>2021 CCPC 桂林 E - Buy and Delete</h2><h3 id="题目描述"><a href="#题目描述" class="headerlink" title="题目描述"></a>题目描述</h3><p>Alice 和 Bob 在有向图 $G$ 上玩游戏。最初图中没有边，Alice 可以先购买一些边加入图中，购买总价不能超过 $c$。之后 Bob 每轮可以删除一个边集 $S$，要求只保留 $S$ 中的边时图是无环的。Bob 不断删除直到图中没有边。</p>
<span id="more"></span>
<p>Alice 希望最大化删除轮数，Bob 希望最小化删除轮数。要求预测最终删除轮数。</p>
<h3 id="输入"><a href="#输入" class="headerlink" title="输入"></a>输入</h3><p>输入包含一个测试用例。</p>
<p>第一行包含三个整数 $n,m,c$，分别表示点数、可购买边数和 Alice 的预算，满足：</p>
<script type="math/tex; mode=display">
2 \le n \le 2000, \quad 1 \le m \le 5000, \quad 1 \le c \le 10^9</script><p>接下来 $m$ 行，每行三个整数 $u_i,v_i,p_i$，表示一条从 $u_i$ 到 $v_i$、价格为 $p_i$ 的有向边。</p>
<h3 id="输出"><a href="#输出" class="headerlink" title="输出"></a>输出</h3><p>输出一个整数，表示删除轮数。</p>
<h3 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h3><p>如果一条边都买不起，答案为 $0$。</p>
<p>如果可以买至少一条边，但买不到任何有向环，那么图是 DAG，一轮就能删完，答案为 $1$。</p>
<p>如果预算内可以买出至少一个有向环，那么答案为 $2$。因此问题可以转化为：判断预算 $c$ 内是否存在有向环。</p>
<p>做法是枚举每个点作为起点，跑最短路寻找回到起点的最小环。如果某个最小环权值不超过 $c$，答案就是 $2$；否则根据是否能买任意一条边判断答案为 $1$ 或 $0$。</p>
<p>时间复杂度约为：</p>
<script type="math/tex; mode=display">
O(nm\log n)</script><h3 id="代码"><a href="#代码" class="headerlink" title="代码"></a>代码</h3><pre><code class="lang-cpp">vector&lt;int&gt; e[N];
int n,m,c,minn,dist[N];
bool st[N];
void Dijkstra(int u)&#123;
    minn=1e18;
    for(int i=1;i,greater &gt;q;
    dist[u]=0;
    q.push(&#123;dist[u],u&#125;);
    while(q.size())&#123;
        auto [dis,ver]=q.top();
        q.pop();
        if(st[ver])continue;
        st[ver]=1;
        for(auto [v,w]:e[ver])&#123;
            if(v==u)minn=min(minn,dis+w);
            if(st[v])continue;
            if(dist[v]&gt;dis+w)&#123;
                dist[v]=dis+w;
                q.push(&#123;dist[v],v&#125;);
            &#125;
        &#125;
    &#125;
&#125;
void solve()&#123;
    cin&gt;&gt;n&gt;&gt;m&gt;&gt;c;
    int ans=0;
    for(int i=1;i&gt;u&gt;&gt;v&gt;&gt;z;
        e[u].pb(&#123;v,z&#125;);
        if(z&lt;=c)ans=1;
    &#125;
    for(int i=1;i&lt;=n;i++)&#123;
        Dijkstra(i);
        if(minn&lt;=c)&#123;ans=2;break;&#125;
    &#125;
&#125;
</code></pre>
]]></content>
      <categories>
        <category>补题</category>
      </categories>
      <tags>
        <tag>图论</tag>
        <tag>最小环</tag>
        <tag>最短路</tag>
        <tag>Dijkstra</tag>
      </tags>
  </entry>
  <entry>
    <title>2016 USP-ICMC</title>
    <url>/2024/03/21/2016%20USP-ICMC/</url>
    <content><![CDATA[<h2 id="Problem-B-Martian-Sunrise"><a href="#Problem-B-Martian-Sunrise" class="headerlink" title="Problem B - Martian Sunrise"></a>Problem B - Martian Sunrise</h2><h3 id="题目描述"><a href="#题目描述" class="headerlink" title="题目描述"></a>题目描述</h3><p>给定 $m$ 行音调，每行有 $7$ 个元素；再给定一行长度为 $n$ 的目标音调。每次可以从 $m$ 行中任选两行，作为当前可用的元素集合去匹配目标音调，求完成匹配所需的最少次数。</p>
<p>约束为 $1 \le m \le 16$，$1 \le n \le 10^4$。</p>
<span id="more"></span>
<h3 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h3><p>可以先枚举所有“两行组合”。组合总数为：</p>
<script type="math/tex; mode=display">
\frac{m(m-1)}{2}</script><p>由于每行只有 $7$ 个元素，组合后的集合规模也不大。接着从目标音调的第一个元素开始向后扫描，并维护当前仍然可行的组合。</p>
<p>如果当前元素不在某个组合中，就把这个组合删掉；如果所有组合都不可行，说明必须重新选择一次组合，此时答案加一，并重新恢复所有组合。</p>
<p>贪心点在于：每次尽量选择能连续匹配更长前缀的组合，不会让最终次数变多。整体复杂度大约在 $3\times 10^7$ 级别，可以接受。</p>
<h3 id="代码"><a href="#代码" class="headerlink" title="代码"></a>代码</h3><pre><code class="lang-cpp">map mp;
int cnt;
set b[300];
bool st[300];
void solve()
&#123;
    int m;
    cin &gt;&gt; m;
    vector v[m];
    for (int i = 0; i &gt; s;
            if (!mp[s])
            mp[s] = ++cnt;
            v[i].pb(mp[s]);
        &#125;
    &#125;

    int n;
    cin &gt;&gt; n;
    vector a(n);
    for (auto &amp;x : a)
    &#123;
        string s;
        cin &gt;&gt; s;
        x = mp[s];
    &#125;

    int idx = 0;
    if (m == 1)
    &#123;
        for (auto &amp;x : v[0])
            b[idx].insert(x);
        idx++;
    &#125;

    for (int i = 0; i  s[N];
queue q;
bitset st;
void bfs(int u, int v)
    &#123;
        int nn = u + h - 1, mm = v + w - 1;
        set::iterator it = s[v].lower_bound(u);
        // cout= u &amp;&amp; t = v &amp;&amp; z &gt; n &gt;&gt; m &gt;&gt; h &gt;&gt; w &gt;&gt; Q;
    ans = n * m;
    for (int i = 1; i &gt; x &gt;&gt; y;
        bfs(x, y);
        cout &lt;&lt; ans &lt;&lt; &quot;\n&quot;;
    &#125;
&#125;
</code></pre>
]]></content>
      <categories>
        <category>补题</category>
      </categories>
      <tags>
        <tag>贪心</tag>
        <tag>DFS</tag>
        <tag>BFS</tag>
        <tag>STL</tag>
      </tags>
  </entry>
  <entry>
    <title>409选拔赛补题</title>
    <url>/2024/03/21/409%E9%80%89%E6%8B%94%E8%B5%9B%E8%A1%A5%E9%A2%98/</url>
    <content><![CDATA[<h2 id="Problem-A-Find-a-Number"><a href="#Problem-A-Find-a-Number" class="headerlink" title="Problem A - Find a Number"></a>Problem A - Find a Number</h2><h3 id="题目描述"><a href="#题目描述" class="headerlink" title="题目描述"></a>题目描述</h3><p>给定两个正整数 $d$ 和 $s$，求一个能被 $d$ 整除，并且数位之和等于 $s$ 的最小正整数 $n$。</p>
<span id="more"></span>
<h3 id="输入"><a href="#输入" class="headerlink" title="输入"></a>输入</h3><p>第一行包含两个正整数 $d$ 和 $s$，满足 $1 \le d \le 500$，$1 \le s \le 5000$。</p>
<h3 id="输出"><a href="#输出" class="headerlink" title="输出"></a>输出</h3><p>如果存在答案，输出满足条件的最小正整数；否则输出 $-1$。</p>
<h3 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h3><p>可以把状态设计为：</p>
<script type="math/tex; mode=display">
(\text{当前模数},\text{当前数位和})</script><p>从一个状态后面追加一位数字 $x$ 后，新模数会变为：</p>
<script type="math/tex; mode=display">
(mod\times 10+x)\bmod d</script><p>数位和会增加 $x$。因此可以在状态图上跑 BFS。由于 BFS 按位数从小到大扩展，并且每次按数字从小到大转移，所以第一次到达状态 $(0,s)$ 时得到的就是最小答案。</p>
<p>同时记录每个状态的前驱和追加的数字，最后从 $(0,s)$ 倒推即可还原答案。</p>
<h3 id="代码"><a href="#代码" class="headerlink" title="代码"></a>代码</h3><pre><code class="lang-cpp">queue&gt; q;
bitset st[N][M];
int s, d;
struct node
&#123;
    short x, y, z;
&#125; path[N][M];
stack stk;
void solve()
&#123;
    cin &gt;&gt; s &gt;&gt; d;
    st[0][0] = 1;
    q.push(&#123;0, 0&#125;);
    path[0][0] = &#123;-1, -1, -1&#125;;
    while (q.size())
    &#123;
        auto [mod, sum] = q.front();
        q.pop();
        for (int i = 0; i &lt;= 9; i++)
        &#123;
            int ver = (mod * 10 + i) % s, su = sum + i;
            if (su &lt;= d &amp;&amp; st[ver][su] == 0)
                q.push(&#123;ver, su&#125;), st[ver][su] = 1, path[ver][su] = &#123;mod, sum, i&#125;;
        &#125;
    &#125;
    if (st[0][d] == 0)
    &#123;
        cout &lt;&lt; -1 &lt;&lt; &quot;\n&quot;;
        return;
    &#125;
    s = 0;
    while (path[s][d].x != -1)
    &#123;
        auto [x, y, z] = path[s][d];
        stk.push(z);
        s = x, d = y;
    &#125;
    while (stk.size())
    &#123;
        cout &lt;&lt; stk.top();
        stk.pop();
    &#125;
&#125;
</code></pre>
]]></content>
      <categories>
        <category>补题</category>
      </categories>
      <tags>
        <tag>图论</tag>
        <tag>同余最短路</tag>
      </tags>
  </entry>
  <entry>
    <title>牛客周赛37</title>
    <url>/2024/03/19/%E7%89%9B%E5%AE%A2%E5%91%A8%E8%B5%9B37/</url>
    <content><![CDATA[<h2 id="E-魔法之森的蘑菇"><a href="#E-魔法之森的蘑菇" class="headerlink" title="E - 魔法之森的蘑菇"></a>E - 魔法之森的蘑菇</h2><h3 id="题目描述"><a href="#题目描述" class="headerlink" title="题目描述"></a>题目描述</h3><p>给定一张网格图，起点为 $S$，终点为 $T$。起初可以任选一个前进方向；除非遇到 <code>*</code>，否则不能改变方向。遇到 <code>*</code> 时，可以选择除“返回刚刚来的方向”以外的其他方向。<code>#</code> 为障碍，<code>.</code> 为普通路径，求从 $S$ 到 $T$ 的最短距离。</p>
<span id="more"></span>
<h3 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h3><p>一开始容易想到普通 BFS：遇到 <code>*</code> 就把三个可选方向都加入队列，遇到 <code>.</code> 就沿原方向继续走。</p>
<p>但这个题的状态不能只记录位置。到达同一个点时，不同的进入方向会影响后续能否转向，因此状态应该是：</p>
<script type="math/tex; mode=display">
(x,y,dir)</script><p>也就是位置加方向。每个点需要维护四个方向上的最短距离，本质上是一个按方向分层的最短路问题。</p>
<h2 id="另一题：按位与-DP"><a href="#另一题：按位与-DP" class="headerlink" title="另一题：按位与 DP"></a>另一题：按位与 DP</h2><h3 id="分析-1"><a href="#分析-1" class="headerlink" title="分析"></a>分析</h3><p>状态可以设计为：</p>
<script type="math/tex; mode=display">
dp[i][j]</script><p>表示考虑前 $i$ 个数时，按位与结果为 $j$ 的最小选择个数。转移为：</p>
<script type="math/tex; mode=display">
dp[i][a_i\&j]=\min(dp[i][a_i\&j], dp[i-1][j]+1)</script><p>由于相同的数进行按位与后结果不会变成 $0$，可以先统计 $0\sim127$ 中出现过的值，并使用滚动数组优化空间。</p>
<p>滚动后的转移为：</p>
<script type="math/tex; mode=display">
dp_1[i\&j]=\min(dp_1[i\&j], dp_0[j]+1)</script><h3 id="E-题代码"><a href="#E-题代码" class="headerlink" title="E 题代码"></a>E 题代码</h3><pre><code class="lang-cpp">int n, m, sx, sy, ex, ey, len[N][N][4], dx[] = &#123;0, 0, 1, -1&#125;, dy[] = &#123;1, -1, 0, 0&#125;;
bool st[N][N][4];
char g[N][N];
queue&gt; q;
void solve()
&#123;
    while (q.size())
        q.pop();
    cin &gt;&gt; n &gt;&gt; m;
    for (int i = 1; i &gt; g[i][j];
            if (g[i][j] == &#39;S&#39;)
                sx = i, sy = j, g[i][j] = &#39;*&#39;;
            if (g[i][j] == &#39;T&#39;)
                ex = i, ey = j, g[i][j] = &#39;*&#39;;
        &#125;
    for (int i = 0; i = 1 &amp;&amp; t = 1 &amp;&amp; z = 1 &amp;&amp; t = 1 &amp;&amp; z = 1 &amp;&amp; t = 1 &amp;&amp; z
</code></pre>
<h3 id="按位与-DP-代码"><a href="#按位与-DP-代码" class="headerlink" title="按位与 DP 代码"></a>按位与 DP 代码</h3><pre><code class="lang-cpp">int dp[2][203],p[220];//滚动数组和记录0~127数存在与否的p
void solve()&#123;
    int n;cin&gt;&gt;n;
    memset(p,0,sizeof p);
    for(int i=1;i&gt;x;
        p[x]++;
    &#125;
    memset(dp,0x3f,sizeof dp);
    for(int i=0;in)cout&lt;&lt;-1&lt;&lt;&quot;\n&quot;;
    else cout&lt;&lt;n-dp[0][0]&lt;&lt;&quot;\n&quot;;//求得组成当前数最小个数，所以抛弃个数为n-dp[0][0]
&#125;
</code></pre>
]]></content>
      <categories>
        <category>补题</category>
      </categories>
      <tags>
        <tag>图论</tag>
        <tag>分层图</tag>
        <tag>动态规划</tag>
        <tag>类背包dp</tag>
      </tags>
  </entry>
</search>
